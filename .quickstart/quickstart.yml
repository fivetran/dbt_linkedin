database_key: linkedin_ads_schema
schema_key: linkedin_ads_database

dbt_versions: ">=1.3.0 <2.0.0"

destination_configurations:
  databricks:
    dispatch:
      - macro_namespace: dbt_utils
        search_order: [ 'spark_utils', 'dbt_utils' ]
